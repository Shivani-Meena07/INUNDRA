fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
avwx-engine>=1.9.0
pyswmm>=1.2.0
python-multipart>=0.0.9
slowapi>=0.1.9
alembic>=1.13.0

# Only needed if DATABASE_URL points at Postgres instead of the
# default SQLite file; safe to remove if staying on SQLite.
psycopg2-binary>=2.9.9

# Dev/test only
pytest>=8.0.0
